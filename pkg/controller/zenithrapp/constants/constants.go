package constants

const (
	// Environment variable for the pod that contains the rules for GET calls
	GETRules = "GET"
	// Environment variable for the pod that specifies the generated service user-friendly name
	ServiceName = "NAME"
)
